# Synthetic test city used by `make city CITY=testcity`.
# Its input data (Sentinel-2 tile, footprints, GHSL, etc.) is generated by
# tools/fetch_test_fixtures.py and placed under ai-dua-mapping/data/raw/
# with the exact naming convention that the framework expects.
country: testland
year: 2025