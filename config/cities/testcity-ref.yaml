# Synthetic test city variant that ships with reference (DUA) polygons, so the
# finetune branch of the framework can be exercised end-to-end. The fixtures
# are generated by tools/fetch_test_fixtures.py.
country: testland
year: 2025